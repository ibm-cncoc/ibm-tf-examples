# Default values for helm chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
label: flask-app-books
namespace: default


configmap: books-app-config

dbSecret: postgres-secret
 